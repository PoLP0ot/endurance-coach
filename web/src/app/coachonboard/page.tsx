import { CoachOnboard } from "@/components/onboarding/coach-onboard";

export const metadata = {
  title: "Set your goal — Endurance Coach",
};

export default function CoachOnboardPage() {
  return <CoachOnboard />;
}
